version: "3.8"

services:
  gitingest:
    image: ghcr.io/coderamp-labs/gitingest:v0.3.1
    container_name: gitingest
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      ALLOWED_HOSTS: "${ENV_ALLOWED_HOSTS:-localhost}"